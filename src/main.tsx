import React from 'react'
import ReactDOM from 'react-dom/client'
import Profile from '../routes/index.ts'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Profile />
  </React.StrictMode>,
)
